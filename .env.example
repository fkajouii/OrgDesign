# Copy this file to .env.local for local development.
# Both values are safe to expose in client-side code (they identify the
# app to Google, they are not secrets) but must still be scoped correctly
# in Google Cloud Console — see README.md for setup steps.

VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_API_KEY=
